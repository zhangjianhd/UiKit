<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhangjian.samp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UiKit">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".slidingblock.SlidingBlockTabActivity" />
        <activity android:name=".slidingblock.SlidingBlockViewPagerActivity" />
        <activity android:name=".slidingblock.SlidingBlockActionActivity" />
        <activity android:name=".scop.SlidingBlockScopeActivity" />
        <activity android:name=".chart.PieChartActivity" />
        <activity android:name=".hobby.HobbySlideActivity" />
    </application>

</manifest>